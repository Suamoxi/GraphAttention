output_root: /scratch/coop/theret/GraphAttention_runs
batch_size: 16
num_workers: 4
max_epochs: 100
device: cuda
pin_memory: true
train_ratio: 0.7
validation_ratio: 0.15
group_metadata_key: source_stem
